public class Lecture {
  public static void main(String[] args) {

    /*
     * SuperClass - also called the parent class
     * Direct superclass - subclass directly inherits
     * indirect superclass - class above the direct superclass in hierarchy
     * class can only have one parent class
     * is-a relationship --> object of a subclass is also an object of a superclass
     * has-a relationship --> objects as member references to other ojects
     * everything from the parent class is made available in the child class EXCEPT constructors
     */

    /*
     * subclass - also called the child class
     * exhibits behaviors of its superclass (specialization)
     */

  }
}
